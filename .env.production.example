# Production Environment Variables Template
# Copy this to .env.production and fill in your values

# Port Configuration (IMPORTANT: Change if ports are already in use!)
APP_PORT=3000          # Application port - change if 3000 is taken (e.g., 3001, 3005, 8080)
DB_PORT=5432           # PostgreSQL port - change if 5432 is taken (e.g., 5433, 5434)

# Database Connection
# IMPORTANT: Update DB_PORT in the URL if you changed it above!
DATABASE_URL="postgresql://postgres:YOUR_SECURE_PASSWORD@10.10.0.3:5432/christmas_feedback?schema=public"

# Node Environment
NODE_ENV=production

# Server Configuration
HOSTNAME=0.0.0.0
PORT=3000

# PostgreSQL (if running in Docker)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=YOUR_SECURE_PASSWORD
POSTGRES_DB=christmas_feedback
